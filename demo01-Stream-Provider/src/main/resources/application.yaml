server:
  port: 8000
spring:
  application:
    name: StreamWithRabbitMQProvider
  rabbitmq:
    addresses: localhost
    port: 5672
    username: guest
    password: guest
  cloud:
    stream:
      bindings:
        MyConsumer:
          destination: pipe
